## Available Scripts

In the project directory, you can run:

### To run the server:

Run `npm start`.<br />
Runs the server on http://localhost:8080.

### To run the client:

Navigate to /property-listings-app and run `npm start`.
Access the client on http://localhost:3000.
